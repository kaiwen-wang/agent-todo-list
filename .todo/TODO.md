Remove notifications from center of top pgae. Gets in way of things.

Any time a note is created/PR is made on worktree, generate Test Criteria

use actual icons included with this app. claude.md

git commits with prefix number

Load the session history into the app itself